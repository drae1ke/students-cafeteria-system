<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reset Your Password - University Cafeteria System</title>
  <link rel="stylesheet" href="/css/reset-password.css">
  <link rel="shortcut icon" href="/img/favicon.png">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
  <div class="loading-overlay">
    <div class="spinner"></div>
  </div>

  <div class="container">
    <h1>Reset Your Password</h1>
    
    <% if(locals.messages && messages.error) { %>
      <div class="alert alert-danger">
        <%= messages.error %>
        <span class="close-btn" onclick="hideError()">&times;</span>
      </div>
    <% } %>
    
    <form id="reset-password-form">
      <div class="form-group password-toggle">
        <label for="new-password">New Password</label>
        <input type="password" id="new-password" required>
        <i class="toggle-password fas fa-eye" onclick="togglePassword('new-password')"></i>
        <div class="error-message" id="new-password-error"></div>
      </div>
      
      <div class="form-group password-toggle">
        <label for="confirm-new-password">Confirm New Password</label>
        <input type="password" id="confirm-new-password" required>
        <i class="toggle-password fas fa-eye" onclick="togglePassword('confirm-new-password')"></i>
        <div class="error-message" id="confirm-new-password-error"></div>
      </div>
      
      <button type="submit">Reset Password</button>
    </form>
  </div>
  <script src="../scripts/reset-password.js"></script>
</body>
</html>